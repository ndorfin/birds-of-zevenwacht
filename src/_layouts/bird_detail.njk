{% set bird = birds[birdId] %}
{% set page_meta = {
	title: bird.name,
	heading: bird.name,
	summary: 'A record of sightings and photos within the suburb of Zevenwacht'
} %}
{% extends "./default.njk" %}
